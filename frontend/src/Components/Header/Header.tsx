import StyledWrapper from "./Header.styled";

function Header(): JSX.Element {
  return (
      <StyledWrapper className="Header" bgColor="#28a745">Header Screen</StyledWrapper>
  );
}

export default Header;
